# Text Adventure Game - Sample Environment Variables
# Copy this file to .env to configure your local environment

# OpenAI API Key for AI-generated narrative content
# Get your API key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Redis URL for caching (optional if REDIS_CACHING_ENABLED is set to False)
# Format: redis://username:password@host:port/db_number
# Default value if not set: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# Optional: Explicitly disable Redis caching
# This can be set in your environment, but it's configured in the app directly
# REDIS_CACHING_ENABLED=false